version: '3.8'

services:
  app:
    image: 407401394535.dkr.ecr.ap-northeast-2.amazonaws.com/dnd/backend/ahaive-backend:${IMAGE_TAG:-latest}
    container_name: ahaive-backend
    ports:
      - "8080:8080"
    env_file:
      - .env
    depends_on:
      - db

  db:
    image: pgvector/pgvector:pg16
    container_name: ahaive-db
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - pgvector-data:/var/lib/postgresql/data

  nginx:
    image: nginx:latest
    container_name: ahaive-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./data/nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./data/nginx/app.conf:/etc/nginx/conf.d/default.conf
      - ./public:/public
      - ./data/certbot/www:/var/www/certbot
      - ./data/certbot/conf:/etc/letsencrypt
    depends_on:
      - app
  certbot:
    image: certbot/certbot
    container_name: certbot
    volumes:
      - ./data/certbot/conf:/etc/letsencrypt
      - ./data/certbot/www:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
volumes:
  pgvector-data:

